#include <stdio.h>
#include <stdlib.h>
#include <string.h>

// LeetCode 3: Longest Substring Without Repeatng Char
int LongestSubstr(char* str) {
    // generate subseq and check repeatng characters.
    
}

int main(void) {
    char input[100];
    scanf("%s", input);
    int res = LongestSubstr(input);

    return 0;
}